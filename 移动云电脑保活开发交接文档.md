# 移动云电脑 ZTE 保活开发交接文档

更新时间：2026-05-02 上午

本文记录当前 Python 复现 ZTE 云电脑连接/保活链路的真实状态，方便后续继续开发。文档不记录完整密码、SOHO token、CSAP accessToken、SPICE session-key。

## 1. 当前结论

目标链路是：

```text
SOHO API 登录
  -> list/v6 获取 userServiceId / remainDurationTime
  -> getFirmAuth 获取 vmUserName / vmPassword / vmId / vmc / cag
  -> CSAP sysConfig / getToken / getDesktopList
  -> CSAP startDesktop / async_query 获取 connectStr
  -> 从 connectStr 解出 -k session-key、hv6、proxy-sport、accessToken
  -> KCP/UDT AUTH_HEAD + AUTH_DATA + SYNACK
  -> TLS 1.3 over KCP
  -> CAG tunnel add_link
  -> SPICE main REDQ + RSA ticket
  -> SPICE display REDQ + RSA ticket
  -> DISPLAY_INIT
  -> 等待 SET_ACK / SURFACE_CREATE / DRAW_COPY / MARK
  -> 响应 PING/PONG 和 ACK，期间定期 SOHO heartbeat
```

当前还没有拿到真实帧画面。代码已经实现到 KCP/TLS/tunnel/SPICE/DISPLAY_INIT 的实验路径，最新实测已越过旧的 SPICE `error 10`，但还没收到 `MAIN_INIT`。

最新实测结果：

- 最新官方 `vdconn.log` 已能解出 `connectStr`，并会自动合并同一轮 `StartSpiceProcess AddConnectParm` 里的 `--sn`、`--otlp-trace-id`、`--otlp-parent-id`。
- SPICE main `LinkReply` 已确认是 RSA-2048 公钥，不是 RSA-1024；解析已修正。
- 使用 fresh 官方日志参数时，SPICE main 已走到 `LinkReply -> RSA-OAEP(empty ticket) -> auth_result=0`。
- 当前卡点是 main auth 成功后，tunnel link 1 收到 `0x2a` close，payload `00000000`。官方日志对应函数是 `deal_proxy_recved_cmd_close`，表示服务端关闭 link，关闭码 0。
- 这次 close 与当时外部脚本挤占/冲突时间重合，不能直接判定为协议失败，需要下一次干净画面会话复测。
- Python 自己走 CSAP `cs_startDesktop.action` 仍返回 `1000100 用户会话已失效，请重新登录`。
- 因此还没有进入 `DISPLAY_INIT -> SURFACE_CREATE/DRAW_COPY/MARK`，`keepalive_logs/` 当前只有 main reply/auth 抓包，还没有帧文件。

## 2. 实现状态矩阵

| 模块 | 状态 | 文件 | 备注 |
|---|---:|---|---|
| SOHO 登录、签名、RSA body 加密 | 已实现 | `cloudpc_protocol.py` | 可登录、可 list、可 getFirmAuth |
| `remainDurationTime` 查询 | 已实现 | `cloudpc_protocol.py` | 用于判断是否真实计时 |
| SOHO heartbeat API | 已实现函数 | `cloudpc_protocol.py` | 还没和 display hold 整合为统一循环 |
| 单文件无画面 CAG 保活 | 已实现 | `cloudpc_server_keepalive_standalone.py` | 可给服务器 cron 单次执行 |
| ZTEC/CAG TCP 三阶段鉴权 | 已实现 | `cloudpc_ztec.py`, `cloudpc_ztec_client.py` | CAG reply 200 已见过 |
| CSAP UAS/CSAP 解密 | 已实现 | `cloudpc_ztec_client.py` | 可解官方 `ZTE_Security_Params` 和 `connectStr` |
| CSAP `getToken/getDesktopList` | 已实现 | `cloudpc_ztec_client.py` | Python 可正常返回 token/list |
| CSAP `startDesktop` | 未跑通 | `cloudpc_ztec_client.py` | 当前返回 `1000100` |
| KCP/UDT AUTH/SYNACK | 已实现 | `cloudpc_ztec_client.py` | 日志/实验都到过 SYNACK |
| TLS over KCP | 已实现 | `cloudpc_ztec_client.py` | 走 `open_udt_tls_connection` |
| tunnel `add_link` | 已实现 | `cloudpc_ztec_client.py` | main/display/inputs/cursor/playback/record |
| SPICE main handshake | 部分实测成功 | `cloudpc_ztec_client.py` | LinkReply RSA-2048、RSA ticket、auth_result=0 已通过 |
| SPICE display handshake | 代码已写，未到达 | `cloudpc_ztec_client.py` | 当前还没收到 MAIN_INIT，display 未开始 |
| `DISPLAY_INIT` | 代码已写，未到达 | `cloudpc_ztec_client.py` | 等 main/display handshake 打通 |
| PING/PONG | 已实现 | `cloudpc_ztec_client.py` | main/display/hold 阶段会回 PONG |
| SET_ACK -> ACK_SYNC | 已实现 | `cloudpc_ztec_client.py` | 收到 SET_ACK 会回 ACK_SYNC |
| 滑窗每 N 条消息回 ACK | 未完整实现 | `cloudpc_ztec_client.py` | 只做了 ACK_SYNC，还需按 window 计数回 ACK |
| 保存第一帧原始数据 | 代码已写，未产出 | `cloudpc_ztec_client.py` | `--save-frame` 会写 `.bin` 和 `.json` |

## 3. 关键阻塞点

当前有两个阻塞点：

1. Python 主动获取 fresh `connectStr/session-key` 仍被 CSAP 拒绝。
2. 使用官方日志里的 fresh 参数时，main auth 已成功，但 auth 后收到 tunnel close，还需干净会话复测。

已确认事实：

- `session-key` 不是 SOHO `getFirmAuth` 字段。
- `session-key` 不是 CSAP `accessToken`。
- 官方 `uSmartView_VDI_Client` 命令行里有 `-k <8字节>`，SPICE LinkMess 只使用前 8 字节。
- `session-key` 来自 CSAP `cs_startDesktop.action` / `cs_startDesktop_async_query.action` 返回的 `connectStr`。
- `connectStr` 解密链路是：

```text
ZTE_Security_Params
  -> AES-256-CBC(UasKey/UasIv) 得到外层 JSON
  -> 外层 JSON connectStr
  -> AES-128-ECB(csap_id) 得到启动命令
  -> 命令内包含 -k、--hv6、--pv6、--proxy-sport、--accessToken
```

Python 已经能解官方日志里的 `connectStr`，也能生成和官方一致的 CSAP 加密 body。问题在于 Python 主动调用 `cs_startDesktop.action` 时，服务端返回：

```text
result=1000100
mesg=用户会话已失效，请重新登录
```

已排除：

- 账号/VM 选错：`zhaoboy* -> da32...`，`zhaoboy3 -> 69a...` 的映射已验证。
- startDesktop body 字段差异：官方 body 解密后和 Python body 结构一致。
- JSON 字段顺序：一致。
- Cookie：带 Cookie、清 Cookie、新 Session 都是 `1000100`。
- SOHO DeviceId/AppType：改成官方样本后仍是 `1000100`。
- HTTP 客户端：用系统 `curl` 发送 Python 生成的同一 body 仍是 `1000100`。

另外，最近一次 fresh 日志会话已经能让 SPICE main auth 返回成功：

```text
main LinkReply: REDQ, error=0, RSA-2048 DER public key
main auth_result: 00000000
next: tunnel CLOSE 0x2a, err_code=0, before MAIN_INIT
```

`0x2a` 已在代码里标记为 `TUNNEL_CMD_CLOSE`。下一步需要在没有其他脚本抢同一云电脑的情况下复测，判断 close 是会话冲突导致，还是 LinkMess/后续 main 初始化仍缺字段。

## 4. 当前可运行命令

### 4.1 服务器无画面单次保活

当前服务器 cron 可先跑无画面脚本：

```bash
cd /Users/zxc/Desktop/移动云电脑
python3 cloudpc_server_keepalive_standalone.py
```

看到类似输出时不是卡住，是脚本在持有 socket：

```text
SOHO login OK
target userServiceId=... vmName=... status=运行中
firmAuth vm=... vmc=... cag=...
CAG auth reply=200 ...
holding CAG socket for 120.0s
keepalive OK
```

服务器定时器示例：

```cron
*/10 * * * * cd /path/to/移动云电脑 && /usr/bin/python3 cloudpc_server_keepalive_standalone.py >> /tmp/cloudpc_keepalive.log 2>&1
```

脚本有锁文件，默认：

```text
/tmp/cloudpc_keepalive.lock
```

上一次未结束时，下一次会跳过，避免重复登录挤线。

### 4.2 打印最新官方连接参数

```bash
python3 cloudpc_ztec_client.py \
  --zte-vdconn-log "移动云电脑.app/Contents/Resources/app.asar.unpacked/node_modules/chuanyunAddOn-zte/ccsdk/mac/uSmartView_VDI_Client.app/Contents/log/vdconn.log" \
  --print-zte-log-session
```

输出会脱敏显示：

```text
decoded ZTE log: vm=... hv6=... kcp_dest_port=5100 proxy_sport=60065 sn=... trace=... parent=... session_key=... accessToken=...
```

### 4.3 尝试用官方日志 session 抓一帧

这条只能用于验证最新日志 session 是否仍可用。旧 session 大概率会失败为 `SPICE main link error: 10`；fresh 会话目前已到过 main auth 成功，但还没过 `MAIN_INIT`。

```bash
UN="$UN" PW="$PW" python3 cloudpc_ztec_client.py \
  --from-firm-auth \
  --use-zte-log-session \
  --zte-vdconn-log "移动云电脑.app/Contents/Resources/app.asar.unpacked/node_modules/chuanyunAddOn-zte/ccsdk/mac/uSmartView_VDI_Client.app/Contents/log/vdconn.log" \
  --save-frame keepalive_logs/latest_frame.bin \
  --kcp-timeout 12 \
  --display-hold-seconds 3
```

### 4.4 Python 主动申请 fresh connectStr 再抓帧

当前这条会卡在 `cs_startDesktop failed: 1000100`，但这是后续修复后的主路径：

```bash
UN="$UN" PW="$PW" python3 cloudpc_ztec_client.py \
  --from-firm-auth \
  --fetch-zte-session \
  --save-frame keepalive_logs/fresh_frame.bin \
  --kcp-timeout 12 \
  --display-hold-seconds 35 \
  --verify-remain-duration
```

## 5. 日志路径和查看命令

官方 ZTE 日志目录：

```bash
export APP="/Users/zxc/Desktop/移动云电脑/移动云电脑.app/Contents/Resources/app.asar.unpacked/node_modules/chuanyunAddOn-zte/ccsdk/mac/uSmartView_VDI_Client.app"
export CLIENT_LOG="$APP/Contents/log/client.log"
export VDCONN_LOG="$APP/Contents/log/vdconn.log"
export CAG_LOG="$APP/Contents/log/cag.log"
```

不要直接执行 `$CAG_LOG`，那是在运行日志文件，会 `permission denied`。正确命令：

```bash
tail -f "$CAG_LOG"
```

本机没有 `rg` 时，用 `grep -E`：

```bash
tail -f "$CLIENT_LOG" | grep -E "deal_kcp|SYNACK|udt ssl connect success|send_tunnel_add_link|all channel|disconnect|error"
```

查看 CSAP startDesktop：

```bash
grep -nE "cs_sysConfig|cs_getToken|cs_getDesktopList|cs_startDesktop|cs_startDesktop_async_query|connectStr" "$VDCONN_LOG" | tail -n 120
```

## 6. IDA / native 关键点

IDA 中重要二进制：

```text
libvdconn.dylib
libEncryptDll.dylib
uSmartView_VDI_Client
```

当前已确认函数：

```text
libEncryptDll.dylib
  AesCbcEncode / AesCbcDecode
    使用 installinfo.ini [PublicKey] UasKey / UasIv
    对 ZTE_Security_Params 做 AES-256-CBC

  AesEncodeForCsap / AesDecodeConnStrFromCsap
    使用 installinfo.ini [PublicKey] csap_id
    对 connectStr 做 AES-128-ECB

libvdconn.dylib
  DoGetSysConfig
  DoGetAccessToken
  DoGetDesktopList
  DoGetConnectStr
  GetConnectStrNoNeedLocalDesktop
  GetConnectStrNoNeedLocalDesktopAsyncQuery
  StartSpiceProcess
  AddConnectParm
  AddOtherParm

uSmartView_VDI_Client
  Application::getSessionKey
  set_otlp_traceparent
  set_monitors_num_in_linkmess
```

CSAP 官方请求顺序：

```text
cs_sysConfig.action
cs_getToken.action
cs_getDesktopList.action
cs_startDesktop.action
cs_startDesktop_async_query.action
```

官方 startDesktop body 已解密确认，字段包括：

```text
RspSecurity, SNcode, accessToken, allowExtUSBPolicy, allowSwitchRap,
assignRelationtoString, connectionType, diskNo, encryption, hostName,
isvm, language, localipandmac, netType, newcharsetparse, newpara,
prover, raptype, requestFrom, supportAsync, supportCustomConfig,
type, upmnew, uuid, verifyTerminalBind, version, vmid, watermarkType
```

## 7. SPICE / tunnel 当前实现细节

`cloudpc_ztec_client.py` 的 `run_display_capture()` 当前做：

```text
open_udt_tls_connection()
  -> KCP/UDT AUTH_HEAD/AUTH_DATA/SYNACK
  -> TLS

build_spice_add_link_packets()
  -> main/playback/record/inputs/display/cursor

SPICE main:
  -> LinkMess
  -> LinkReply
  -> auth_type + RSA-OAEP(empty ticket)
  -> auth_result
  -> 等 MAIN_INIT
  -> SET_ACK 回 ACK_SYNC
  -> PING 回 PONG

SPICE display:
  -> LinkMess(connection_id = MAIN_INIT session id)
  -> LinkReply
  -> auth_type + RSA-OAEP(empty ticket)
  -> auth_result
  -> DISPLAY_INIT
  -> 等 display 消息
  -> SET_ACK 回 ACK_SYNC
  -> PING 回 PONG
  -> MARK 后写 capture 文件
```

`DISPLAY_INIT` 当前参数：

```text
pixmap_cache_id = 1
pixmap_cache_size = 20MB
glz_dictionary_id = 1
glz_dictionary_window_size = 8MB
```

还需要补：

- 根据 SET_ACK 的 `window` 做滑窗计数，每收到 window 条消息回 `ACK`。
- display hold 阶段定时调用 SOHO `heartbeat/v2`。
- MARK 前如果出现 `SURFACE_CREATE` / `DRAW_COPY`，解析并保存可查看 PNG，而不只是保存原始 `.bin`。

## 8. 验证标准

真正成功的标准不是只连上 CAG，也不是只 TLS 成功，而是至少满足以下之一：

1. display 通道收到 `SURFACE_CREATE` / `DRAW_COPY` / `MARK`。
2. `--save-frame` 产出非空 display 原始数据和 JSON 消息列表。
3. 保持 60 秒以上后，`list/v6` 的 `remainDurationTime` 下降。

你之前观察到 `remainDurationTime` 大约满足 60 秒才会下降，这个判断方式是对的。

## 9. Python 版本和依赖

Python `3.9.25` 可以作为目标版本。当前代码已经避免了 Python 3.10 专属的 `A | B` 类型语法。

依赖：

```bash
python3 -m pip install requests cryptography
```

本机最近验证：

```bash
python3 -m py_compile cloudpc_ztec.py cloudpc_ztec_client.py cloudpc_protocol.py cloudpc_server_keepalive_standalone.py test_cloudpc_ztec.py
python3 -m unittest -v test_cloudpc_ztec.py
```

结果：编译通过，13 个单测通过。

## 10. 当前文件说明

```text
cloudpc_protocol.py
  SOHO HTTP 控制面：登录、签名、list、getFirmAuth、heartbeat。

cloudpc_server_keepalive_standalone.py
  服务器 cron 单文件版本；当前只做 SOHO + CAG 无画面保活测试。

cloudpc_ztec.py
  ZTEC/KCP/tunnel/SPICE 基础包结构。

cloudpc_ztec_client.py
  实验客户端：CSAP 解密、KCP/TLS、tunnel add_link、SPICE main/display、DISPLAY_INIT、保存帧。

test_cloudpc_ztec.py
  单元测试。

ZTE_PROTOCOL_FINAL.md
  更长的协议逆向说明，部分结论早于当前实现，需要以本文为准。

ANALYSIS.md
  早期分析记录，部分内容已过时。
```

## 11. 下一步建议

优先顺序：

1. 在干净官方画面会话中重跑 `--use-zte-log-session --save-frame`，确认 main auth 后是否还会 `TUNNEL_CMD_CLOSE err_code=0`。
2. 如果仍 close，对比官方 LinkMess 705 字节 body 的固定字段、capability、monitor/workmode 参数。
3. 继续查 `cs_startDesktop.action` 返回 `1000100` 的前置条件，目标是 Python 自己拿 fresh `connectStr`。
4. 对比 official native 调用 `SohoSdk_DoConnectDesktop` 前后是否有额外 SOHO API 或本地状态写入。
5. 跑通 display 后，再补完整 ACK window、SOHO heartbeat 循环和 PNG 转储。
